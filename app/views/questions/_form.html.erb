<%= form_with model: [@test, @question] do |f| %>
  <%= render 'shared/errors', resource: @question %>
  <p>
    <%= f.label :body%>
    <%= f.text_field :body%>
  </p>
  <p><%= f.submit %></p>
<% end %>
