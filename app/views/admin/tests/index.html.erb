<h1> <%= t('.header') %> </h1>
<p> <%= link_to t('.new_test'), new_admin_test_path %> </p>
<table>
  <tr><th><%= t('tests.index.heading.name_test') %></th><th><%= t('tests.index.heading.number_questions') %></th></tr>
  <%= render @tests %>
</table>
