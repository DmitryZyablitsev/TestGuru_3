<div class="row">
  <h1> <%= t('.header') %> </h1>
  <p> <%= link_to t('.new_test'), new_admin_test_path %> </p>
  <table class="table table-hover">
    <tr>
      <th><%= t('tests.index.heading.name_test') %></th>
      <th><%= t('tests.index.heading.number_questions') %></th>
      <th><%= t('tests.index.heading.actions') %></th>
    </tr>
    <%= render @tests %>
  </table>
</div>
