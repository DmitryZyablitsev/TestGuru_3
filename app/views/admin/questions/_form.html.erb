<%= form_with model: [:admin, @test, @question] do |f| %>
  <%= render 'shared/errors', resource: @question %>
  <p>
    <%= f.label :body%>
    <%= f.text_field :body%>
  </p>
  <p><%= f.submit %></p>
<% end %>
