<div class="row">
  <h1> <%= t('.header') %> </h1>

  <table class="table table-hover">
    <tr>
      <th><%= t('.heading.name_test') %></th>
      <th><%= t('.heading.number_questions') %></th>
      <th><%= t('.heading.actions') %></th>    
    </tr>
    <%= render @tests %>
  </table>
</div>
